require('./bootstrap');

import Vue from 'vue';

// Import y config vue router
import VueRouter from 'vue-router';
Vue.use(VueRouter);


// Crear instancia de Vue

const app = new Vue({
    el: '#app',
    
});